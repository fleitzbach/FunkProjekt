# FunkProjekt

## Setup

```
cd backend
pip install -r requirements.txt
cd ../frontend
npm install
```

## Start (dev Server)

```
(im frontend Ordner)
npm run dev
cd ../backend
uvicorn main:app --reload
```
